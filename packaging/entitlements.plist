<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Under the hardened runtime the microphone is refused outright
	     without this, before macOS would even ask. -->
	<key>com.apple.security.device.audio-input</key>
	<true/>
	<!-- System Events raises the Boss window; a computer-use task can
	     script the apps it was allowed to drive. -->
	<key>com.apple.security.automation.apple-events</key>
	<true/>
</dict>
</plist>
