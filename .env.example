# Copy to .env and fill in. .env is gitignored.

# Required: OpenAI project key with GPT Live access.
OPENAI_API_KEY=

# Claude auth is optional. Left blank, the Agent SDK uses the login you
# already created with `claude /login`. Set one of these only to override it.
CLAUDE_CODE_OAUTH_TOKEN=
ANTHROPIC_API_KEY=
